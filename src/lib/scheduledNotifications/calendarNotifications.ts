import type { EventTimeSlot, Event, EventRepeatType } from "$components/calendar/event/Event";

async function setNotif(notifyTime?: number, : Date, repeat: EventRepeatType, repeatInterval?: number, repeatUntil: Date ){
    
}